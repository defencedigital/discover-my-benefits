const COC_NAMESPACES = Object.freeze({
  SERVING_TYPE: 'profile.servingtype',
  SERVICE_COMMITMENT_TYPE: 'coc.service_commitment_type',
  TRAINING_PHASE: 'coc.trainingphase',
  RANK: 'coc.rank',
});

const ID_LENGTH = 36;

const COC_FORM_ID = 'change-of-circumstances';

export { ID_LENGTH, COC_NAMESPACES, COC_FORM_ID };
